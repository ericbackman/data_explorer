"""NHL data sub-project — game index + boxscores from the free NHL API
(api-web.nhle.com / api.nhle.com), mirroring nba/ and nfl/.

Modules: api (client + parsers), build (schema + games index),
boxscores (per-game skater/goalie backfill), pbp (play-by-play backfill),
series (playoff-series derivation). See nhl/README.md.
"""
