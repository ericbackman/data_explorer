"""Multi-competition soccer data: ESPN broad tier + StatsBomb deep tier.

Mirrors the pga/ package shape (tournament -> rounds -> holes becomes
competition -> match -> events). Run from the data_explorer repo root, e.g.
    python -m soccer.scrape --competition fifa.world
"""
