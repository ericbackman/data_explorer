"""podcast-lab — a queryable semantic layer over podcast transcripts.

Ingest RSS feeds, acquire transcripts via a free-first waterfall, and store
everything in a local SQLite DB so podcast questions ("how often does Bill
Simmons say Boston, by year?") become plain SQL + a mention-counting function.
"""
